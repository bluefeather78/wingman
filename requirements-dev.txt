# Test-only dependencies. Not installed on Render (buildCommand uses requirements.txt).
# CI installs both: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27        # FastAPI's TestClient transport (integration tests)
